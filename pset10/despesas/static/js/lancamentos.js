
var handleEdit = function(id, idDespesa, idReceita){
    
    if (idDespesa === 0)
        alert('Todo handleEdit idReceita = ' + idReceita);
    else
        alert('Todo handleEdit idDespesa = ' + idDespesa);

}

var handleDelete = function (id, idDespesa, idReceita){
    if (idDespesa === 0)
        alert('Todo handleDelete idReceita = ' + idReceita);
    else
        alert('Todo handleDelete idDespesa = ' + idDespesa);
}
