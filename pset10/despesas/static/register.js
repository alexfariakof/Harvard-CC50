

var handleIncluir = function (e) {
    if (e.target.id === 'btnIncluir' && this.document.title === 'Categorias')
        alert('Incluuir Nova Categoria');
    if (e.target.id === "btnIncluir" && this.document.title === 'TipoCategorias')
        alert('Incluir Novo Tipo de Categoria');
    if (e.target.id === 'btnIncluir' && this.document.title === 'Despesas')
        alert('Incluir Nova Despesa');
    if (e.target.id === 'btnIncluir' && this.document.title === 'Receitas')
        alert('Incluir Nova Receita');
}


var handleAlterar = function (e) {
    if (e.target.id === "btnAlterar" && document.title === 'Categorias')
        alert('Alterar Nova Categoria');
    if (e.target.id === "btnAlterar" && document.title === 'TipoCategorias')
        alert('ALterar Novo Tipo de Categoria');
    if (e.target.id === 'btnAlterar' && document.title === 'Despesas')
        alert('Alterar Nova Despesa');
    if (e.target.id === 'btnAlterar' && document.title === 'Receitas')
        alert('Alterar Nova Receita');
}

var handleExcluir = function (e) {
    if (e.target.id === 'btnExcluir' && document.title === 'Categorias')
        alert('Excluir Nova Categoria');
    if (e.target.id === 'btnExcluir' && document.title === 'TipoCategorias)
                alert('Excluir Novo Tipo de Categoria');
    if (e.target.id === 'btnExcluir' && document.title === 'Despesas')
        alert('Excluir Nova Despesa');
    if (e.target.id === 'btnExcluir' && document.title === 'Receitas')
        alert('Excluir Nova Receita');
}


var handleExcluir = function (e) {
    if (e.target.id === "btnCategoria")
        alert('Excluir Nova Categoria');
    if (e.target.id === "btnTipoCategoria")
        alert('Excluir Novo Tipo de Categoria');
    if (e.target.id === "btnDespesa")
        alert('Excluir Nova Despesa');
    if (e.target.id === "btnReceita")
        alert('Excluir Nova Receita');
}


